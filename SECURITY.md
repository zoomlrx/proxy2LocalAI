# 安全策略

Proxy2LocalAI 同时包含浏览器扩展和本地 Bridge，因此安全边界比普通前端项目更敏感。请把它视为一个“只允许明确配置目标”的本机自动化工具。

## 支持版本

当前项目仍处于 `0.x` 阶段。安全修复会优先合入 `master`，并在下一个 patch 版本发布。

| 版本 | 状态 |
| --- | --- |
| 0.1.x | 支持 |

## 报告漏洞

如果你发现以下问题，请优先私下报告维护者，而不是公开 issue：

- 未授权访问 Bridge 管理接口。
- token、请求头、配置文件或本地路径泄露。
- 扩展生成了超出用户配置范围的代理规则。
- Provider 命令参数导致非预期本地命令执行。
- 配置导入可以绕过校验或写入危险值。

当前仓库尚未配置 GitHub 私有漏洞报告时，请通过 GitHub 个人资料中提供的联系方式联系维护者，并附上：

- 影响版本或 commit。
- 最小复现步骤。
- 预期行为和实际行为。
- 你认为可能的影响范围。

## 默认安全边界

- Bridge 只监听 `127.0.0.1`。
- `/doctor`、`/admin/profiles` 和 `/proxy/<profileId>` 都需要本地 token。
- 扩展只为启用的 profile 创建 DNR 动态规则。
- 默认不启用 Claude `--dangerously-skip-permissions` 和 Codex `--full-auto`。
- 如需无人值守 CLI 模式，必须显式设置 `PROXY2LOCALAI_ALLOW_DANGEROUS_CLI=true`。

## 维护者响应目标

- 3 天内确认收到报告。
- 7 天内给出初步影响判断。
- 高危问题优先发布修复版本，并在修复后补充公开说明。
